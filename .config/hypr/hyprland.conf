# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

source = ~/.config/hypr/configs/monitors.conf

# Define your workspaces (9 total)
workspace = 1, default:true
workspace = 2
workspace = 3
workspace = 4
workspace = 5
workspace = 6
workspace = 7
workspace = 8
workspace = 9

###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

source = ~/.config/hypr/configs/window-theme.conf

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$menu = Drun
$browser = firefox
$floatingKitty = kitty --class floating-kitty
$yazi = kitty --class yazi-float -e yazi
$fetch = kitty --class sysfetch-float -e "$HOME/.config/hypr/misc/sysfetch"
$colorpicker = hyprpicker -a
$themeselector = ThemeSelector
$musicplayer = kitty --class ncmpcpp-float ncmpcpp

####################
# IMPORTED CONFIGS #
####################

source = ~/.config/hypr/configs/bindings.conf

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = WayBar
exec-once = awww-daemon --format xrgb
exec-once = Swaync
exec-once = SlideShow

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

# Add custom launchers and bin directories to PATH
env = PATH,$PATH:$HOME/.config/hypr/launchers:$HOME/.config/hypr/bin:$HOME/.config/hypr:$HOME/.config/hypr/misc

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

source = ~/.config/hypr/window-themes/$theme.conf

#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = -0.2 # -1.0 - 1.0, 0 means no modification.
    accel_profile = flat

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

source = ~/.config/hypr/configs/window-rules.conf

# Dynamic monitor config
source = /home/kaede/.config/hypr/configs/monitors.conf
